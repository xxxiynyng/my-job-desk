# 리팩토링 규칙 (2026-07-29 세션에서 확정)

> 제1원칙: **기존 동작과 UI 렌더 결과의 완전한 보존.** 아래 규칙보다 이 원칙이 항상 우선한다.
> 규칙을 지키려다 렌더가 1px이라도 달라진다면, 규칙을 어기고 현행을 유지한 뒤 보고서에 기록한다.

## R1. 배치 — 어디에 두는가

| 위치 | 담는 것 | 판정 기준 |
|---|---|---|
| `src/app/` | 라우터 셸, 전역 프로바이더 | 앱 진입에만 관계 |
| `src/features/<기능>/` | 한 기능에만 쓰이는 전부 | **소비 기능이 1개** |
| `src/components/` | 2개 이상 기능이 쓰는 UI | **소비 기능이 2개 이상** |
| `src/data/` | 시드·정본 상수·도메인 스토어 | 화면을 모른다 |
| `src/lib/` | 순수 유틸 | React를 모른다 |
| `src/hooks/` | 2개 이상 기능이 쓰는 훅 | 소비 기능이 2개 이상 |

**의존 방향은 한쪽으로만 흐른다:** `app → features → components → hooks/lib/data`.
역방향 import는 위반이다. (예: `data/`가 `components/`를 import → 금지)

## R2. 기능 폴더 내부

```
features/<기능>/
  <Name>Page.tsx      default export. 화면 조립만
  components/         이 기능 전용 UI (named export)
  model/              타입 · 상수 · 시드 · 순수 파생 (React 미포함)
```

**예외 1건:** `features/experiences/story/` — 소재·역량 도메인 레이어는 폴더를 통째로 유지한다.
"서버가 붙으면 `api.ts` 한 줄만 교체"라는 경계선이 이 레이어의 존재 이유이고,
`model/`·`components/`로 쪼개면 그 교체 지점이 세 곳으로 흩어진다. (기획서 1.17-A · 결정 #14)

## R3. import 위치

**모든 import는 파일 최상단 한 블록에만 둔다.** 함수 사이나 파일 끝의 import는 호이스팅으로 동작하지만,
"이 파일이 무엇에 의존하는가"를 파일 머리에서 읽을 수 없게 만든다.

## R4. export

- 페이지: `export default` 하나만. **재수출 배럴 노릇을 하지 않는다** (정본이 페이지에 있다는 착시를 만든다)
- 컴포넌트·유틸: named export
- **파일 밖에서 안 쓰는 심볼에는 `export`를 붙이지 않는다** — public API인 척하는 내부 함수는 매번 grep을 부른다

## R5. 정본(SSOT)

같은 값·같은 타입을 두 곳에 적지 않는다. 이미 정본이 있으면 **파생하거나 import한다.**

| 정본 | 담는 것 |
|---|---|
| `data/jobStatus.ts` | 전형 단계·최종 결과 |
| `lib/dday.ts` | D-day 계산 |
| `lib/storage.ts` | localStorage 안전 접근 |
| `lib/designTokens.ts` | 폰트 크기 토큰 |
| `components/table/tableState.ts` | 테이블 상태 갱신 규칙 |

**단, 값이 미묘하게 다르면 통합하지 않는다.** 예: `ddayLabel`의 0일 표기가 `"D-Day"`와 `"오늘"`로 갈리는 것은
중복이 아니라 서로 다른 요구다. 이런 것은 통합 금지 주석을 남긴다.

## R6. 통합 판정 — 무엇을 합치고 무엇을 두는가

| 판정 | 조건 | 처리 |
|---|---|---|
| **합친다** | 렌더 DOM·클래스·실행 결과가 **완전히 동일** | 공용화 |
| **일부만 합친다** | 공통 코어는 같고 주변이 다름 | **코어만** 뽑고 차이는 호출부에 남긴다 |
| **두고 기록한다** | 경계값·시간대·문구·이펙트 순서가 다름 | 손대지 않고 보고서에 기록 |

**훅 추출은 기본적으로 "두고 기록한다"에 넣는다** — `useState`/`useEffect`가 훅 안으로 들어가면
이펙트 등록 순서와 첫 렌더 내용이 바뀔 수 있다. 순수 함수·팩토리 추출만 안전으로 본다.

## R7. 검증

각 단계마다 `tsc -p tsconfig.app.json --noEmit` 신규 에러 0을 확인하고 다음 단계로 넘어간다.
빌드·브라우저 실행 검증은 이번 범위에서 금지되므로, **타입으로 잡히지 않는 변경은 애초에 하지 않는다.**
