# 정리 자율주행 — 로컬·GitHub·Vercel 엉킴 해소 (2026-07-05)

> 사용법: 터미널에서 아래로 Claude Code를 열고, 구분선 아래 전체를 붙여넣으세요.
> ```bash
> cd "/Users/xxxiynyng/Downloads/My_Job_Desk 19.25.06"
> claude --permission-mode acceptEdits
> ```

---

너는 지금부터 Pickd 레포에서 **정리 전용 자율주행**을 수행한다. 대원칙: **내용은 보존하고(삭제는 git 히스토리에 남는 것만), 불필요한 중복·잔재만 정리한다.** 파괴적 작업(force push, reset --hard, 히스토리 재작성)은 금지. 시작 전 `git tag pre-cleanup-2026-07-05`.

## 현재 진단 (이걸 검증부터 하라)

- 레포 워킹트리에서 `docs/` 폴더가 사라짐. 그러나 커밋 `3deb1f0`(리포트 4종 git mv)과 `847d4da`(작업문서 3종 반입)에 내용이 있음. `git log --oneline -15`와 `git show --stat 3deb1f0 847d4da`로 확인하고, 사라진 원인(이후 커밋에서 삭제됐는지, 언트래킹인지)을 `git log --diff-filter=D -- docs/`로 짚어라.
- 구 Cowork 폴더 `/Users/xxxiynyng/Claude/Projects/Pickd Design/`에 md 4개가 남아 있음: `CLAUDE.md`(구 사본), `토큰_마이그레이션_계획.md`(**레포 사본보다 최신** — line-height 회귀 경고 문단이 추가돼 있음), `터미널_지시_토큰+프로필.md`, `PICKD_자율주행_프롬프트.md`.
- GitHub: Pages는 이미 비활성화됨(빨간 X 해결). 잔여 환경 3개(github-pages, Production–pickd-seven, Production–my-job-desk)가 Deployments 사이드바에 남아 있음 — 이건 웹 UI에서만 지울 수 있으니 코드로 건드리지 말고 보고에만 남겨라.
- Vercel: 잉여 프로젝트 2개는 사용자가 이미 삭제. "pickd" 프로젝트 하나만 남아 pickd-seven.vercel.app 서빙 — 정상, 손대지 말 것.

## 작업 1 — 교훈 승격 (삭제 전에 딱 하나만 보존)

구 폴더 `/Users/xxxiynyng/Claude/Projects/Pickd Design/토큰_마이그레이션_계획.md`(최신본)에 있는 **line-height 회귀 경고**를 레포 `CLAUDE.md`의 "디자인 토큰 규칙" 섹션에 한 줄로 승격한다:

```
- **토큰에 line-height를 싣지 않는다(크기만).** 튜플로 lineHeight를 지정하면 배지·D-day·카운트처럼 고정 높이 안에서 상속 줄간격에 의존하던 요소가 부풀어 보이는 회귀 발생(2026-07-05 실측). 줄간격 토큰화는 SSOT에 먼저 정의 후 별도로.
```

커밋: `docs(claude): line-height 토큰 금지 규칙 승격`.

## 작업 2 — 일회용 문서 전부 삭제 (사용자 승인됨, git 히스토리 보존)

1. docs/ 소실 원인만 진단해 보고에 남긴다: `git log --diff-filter=D --oneline -- docs/`. **복구는 하지 않는다** — 사용자가 일회용 문서 삭제를 승인했고, 내용은 커밋 `3deb1f0`·`847d4da` 히스토리에서 언제든 `git show`로 볼 수 있다.
2. docs/가 워킹트리에 아직 있으면 `git rm -r docs/`로 삭제. 이미 없으면 스킵.
3. 구 폴더 `/Users/xxxiynyng/Claude/Projects/Pickd Design/`의 md 4개(CLAUDE.md 사본·토큰 계획·터미널 지시·자율주행 프롬프트)를 삭제한다. 삭제 권한이 없으면 각 파일 내용을 `# ⚠️ 이관·폐기됨 — 정본은 코드 레포 CLAUDE.md (2026-07-05)` 한 줄로 비운다.
4. 삭제 전 안전 확인 1회: 레포 `CLAUDE.md`와 구 폴더 `CLAUDE.md`를 diff해서 구 폴더에만 있는 규칙이 있으면 레포에 병합 후 삭제.
5. 커밋: `chore(docs): 일회용 문서 정리(내역은 git 히스토리 보존)`.

## 작업 3 — 이 파일 자체 정리

이 파일(`CLEANUP_2026-07-05.md`)도 실행이 끝나면 `git rm`으로 삭제한다(일회용). 커밋: `chore(docs): 정리 프롬프트 폐기`.

## 작업 4 — 최종 검증·보고

1. `npx tsc -p tsconfig.app.json --noEmit`(에러 0) + `npm run build` 통과 확인 (코드 변경은 없어야 하지만 안전 확인).
2. `git push`.
3. 최종 보고에 포함:
   - docs/ 소실 원인(어느 시점·어느 커밋/동작으로 사라졌는지).
   - 삭제·정리된 파일 목록과 커밋 해시 (+ 필요 시 복원 명령: `git show <hash>:docs/tasks/<파일>`).
   - **사용자가 웹에서 직접 할 것 2건**: ① GitHub 레포 → Deployments → Manage environments에서 잔여 환경 3개(github-pages, Production–pickd-seven, Production–my-job-desk) 삭제(기록 정리용, 기능 영향 없음) ② 확인은 항상 `pickd-seven.vercel.app`으로.

## 하지 말 것

- Vercel "pickd" 프로젝트·도메인 설정 변경 금지.
- src/ 코드 수정 금지(이번엔 문서·구조 정리만).
- 히스토리 재작성(rebase·force push) 금지 — 삭제해도 내역은 커밋 히스토리로 보존되는 것이 전제다.

지금 `git tag pre-cleanup-2026-07-05` → 진단 검증 → 작업 1부터 시작하라.
